package exlcm;

struct heartbeat_message {
    int64_t timestamp;
    int8_t mode;
    int8_t botUID;
    boolean isMoving;
    boolean apply_brakes;
}

struct sensor_message {
    int64_t timestamp;
    boolean detected_obstacle;
}